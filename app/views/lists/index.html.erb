<!-- app/views/lists/index.html.erb -->
<div class="container">
  <h1>Listes de films</h1>
  <div class="card-columns">
    <% @lists.each do |list| %>
      <div class="card">
        <div class="card-body">
          <h5 class="card-title"><%= link_to list.name, list_path(list) %></h5>
        </div>
      </div>
    <% end %>
  </div>
  <%= link_to "Créer une liste de films", new_list_path, class: "btn btn-flat mt-3" %>
</div>
